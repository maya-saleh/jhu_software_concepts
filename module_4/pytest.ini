[pytest]
addopts = --cov=src --cov-fail-under=100
testpaths = tests
markers =
    web: web route/page tests
    buttons: button/endpoint tests
    analysis: analysis page formatting tests
    db: database-related tests
    integration: end-to-end/integration tests

